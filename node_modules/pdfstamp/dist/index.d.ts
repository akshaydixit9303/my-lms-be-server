#!/usr/bin/env node
declare type StampArgs = {
    input: string;
    signature: string;
    output?: string;
    debug?: boolean;
    bottom?: string;
    left?: string;
    right?: string;
    top?: string;
    page?: string;
    pageSize?: string;
    zoom?: string;
};
export declare function stamp({ debug, bottom, left, right, top, input, output, page, pageSize, signature, zoom, }: StampArgs): Promise<void>;
export {};
